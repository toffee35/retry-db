[project]
name = "retry-db"
version = "0.0.1"
requires-python = ">=3.14"
dependencies = [
    "granian[uvloop]==2.7.2",
    "msgspec==0.20.0",
    "structlog==25.5.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
